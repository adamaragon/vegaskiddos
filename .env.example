# Airtable — leave blank to run on built-in seed data.
# Create a base with an "Events" table (see README for the schema),
# then a personal access token at https://airtable.com/create/tokens
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=Events
